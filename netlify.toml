[build]
  base = "affirmations-tanstack"
  command = "vite build"
  publish = "dist/client"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["dist/**"]

[[rewrites]]
  from = "/*"
  to = "/.netlify/functions/server"
